name: weather_app
description: A Flutter app for weather information
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.14.0
  provider: ^6.0.0
  animated_text_kit: ^4.2.1  # Added animated_text_kit dependency
  weather_provider: ^1.0.0  # Assuming this is the correct version

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
